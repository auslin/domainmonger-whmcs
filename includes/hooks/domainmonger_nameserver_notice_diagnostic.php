<?php
/**
 * Retired diagnostic hook from patch 822.
 * Left intentionally inert by patch 823 so installing this patch disables
 * the temporary dmnsdiag=822 output without requiring manual deletion.
 */

if (!defined('WHMCS')) {
    die('This file cannot be accessed directly');
}
