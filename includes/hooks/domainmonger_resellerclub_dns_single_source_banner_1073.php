<?php
/**
 * DomainMonger cleanup stub.
 *
 * Former visible-banner diagnostic hook 1073 for ResellerClub/native DNS testing.
 * Kept as an inert file so installs overwrite any earlier diagnostic version.
 */
if (!defined('WHMCS')) {
    die('This file cannot be accessed directly');
}

// Intentionally no hooks registered.
