<?php
/**
 * Multi Bulk Domain Updater does not require global hooks.
 *
 * This intentionally replaces the obsolete sample hook callbacks bundled with
 * the original addon, which referenced functions that did not exist.
 */

if (!defined('WHMCS')) {
    die('This file cannot be accessed directly');
}
