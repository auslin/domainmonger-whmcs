# ClouDNS WHMCS v1.9 DM Main +Add Alignment Notes

Baseline used: `ClouDNS_WHMCS.v1.9_DM_ADVANCED_MENU_ORDER_install_only.zip`

## Change

- Updated `templates/records.tpl` so the main DNS Records table header `+Add` button right-aligns within the action column.
- Reduced the DataTables disabled-sort header right padding for the `+Add` action header cell so it aligns with the record action icon cluster below it.

## Scope

- UI/CSS-only change.
- No PHP logic changed.
- No API calls changed.
- No menu behavior changed.
