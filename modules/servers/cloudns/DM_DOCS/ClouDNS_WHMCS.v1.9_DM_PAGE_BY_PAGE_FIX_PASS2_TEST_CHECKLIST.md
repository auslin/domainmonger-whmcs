# Test Checklist

## DNS Records
- Host toggle button is more compact.
- Points To mode still fits and is readable.
- Toggle still switches modes.
- Search still works in both modes.

## Mail Forwards
- `+Add` is hard-right aligned in the header/action column.
- `+Add` visually matches DNS Records alignment.
- Existing edit/delete icons remain aligned.

## Statistics
- `Last 30 Days` and `Yearly Statistics` appear as toggle buttons.
- Active selected view is clearly highlighted.
- Both statistics buttons still work.

## Zones List
- `Actions` is spelled correctly.
- `Actions` text aligns with the Manage/Delete action column.
- `+Add` right edge lines up with the Delete button right edge.
- Counter remains next to `+Add`.
- Manage/Delete still work.

## Regression
- Pass 1 fixes for Zone Transfers, Import, and Export remain unchanged.
- No DNS/API behavior changed.
