# Test Checklist

## Zones List
- Page title says `Zones List`.
- Counter appears on the right next to `+Add`.
- Counter format looks like `Zones: 3/10`.
- `+Add` right edge lines up with the Manage/Delete action column.
- Table header says `Actions`.
- `Actions` text lines up with the Manage/Delete button area.
- Manage/Delete buttons remain aligned and readable.
- Mobile action menu still appears correctly.

## Regression
- Add New DNS Zone still opens correctly.
- Manage still opens DNS Records for the selected zone.
- Delete still shows the confirmation prompt.
- No DNS/API behavior changed.
