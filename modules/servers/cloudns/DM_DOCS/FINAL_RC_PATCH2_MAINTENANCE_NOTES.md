# Final RC Patch 2

Baseline: `ClouDNS_WHMCS.v1.9_DM_FINAL_RC_PATCH1_install_only.zip`

## Changes

### Statistics
- Updated Statistics table sort arrows to match the DNS Records page.
- Uses stacked `▲` / `▼` arrows.
- Uses the same active sorted-column orange header treatment.
- Uses the same active/inactive arrow contrast.
- Moved the total Requests indicator to the right side of the Statistics header row.

## Notes
No DNS/API behavior was intentionally changed.
