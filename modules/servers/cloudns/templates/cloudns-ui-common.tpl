{*
Compatibility shim.

This file intentionally contains no runtime CSS or markup.

It prevents Smarty from crashing if an older/stale compiled template or an incomplete
server upload still references cloudns-ui-common.tpl.

Do not reintroduce shared runtime template includes here without WHMCS runtime testing.
*}
